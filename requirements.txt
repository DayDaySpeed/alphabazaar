# Buyer agent
anthropic>=0.69
httpx>=0.27
jinja2>=3.1
rich>=13.7
python-dotenv>=1.0
pydantic>=2.6

# Seller agents (x402-paywalled HTTP services)
fastapi>=0.110
uvicorn[standard]>=0.29

# Real Binance MCP client (BINANCE_MODE=live). Safe to keep installed for mock too.
mcp>=2.1
